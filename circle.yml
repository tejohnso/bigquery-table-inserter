deployment:
  staging:
    branch: /(feature|fix|chore).*/
    commands:
      - echo "nothing to deploy on staging branch"
  production:
    branch: master
    commands:
      - mvn appengine:update -Dappengine.appId=client-side-events -Ddeploy.version=$CIRCLE_BUILD_NUM -Ddeploy.module=default
